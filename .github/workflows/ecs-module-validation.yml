name: AWS ecs Module Validation

on:
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'modules/**'
      - '.github/workflows/**'

jobs:
  terraform-module-validation:
    name: Terraform Module Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.4"
  
      - name: Terraform Format Check
        id: fmt
        run: |
          cd modules/ecs
          terraform fmt -check -recursive
        continue-on-error: false

      - name: Terraform Init
        id: init
        run: |
          cd modules/ecs
          terraform init -backend=false
        continue-on-error: false

      - name: Terraform Validate
        id: validate
        run: |
          cd modules/ecs
          terraform validate
        continue-on-error: false

  opa-terraform-module-validation:
    name: OPA Terraform Module Policy Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup OPA
        uses: open-policy-agent/setup-opa@v2
        with:
          version: latest

      - name: OPA Check
        id: opa-check
        run: |
          opa check modules/ecs/policy/main.rego modules/ecs/policy/test.rego
        continue-on-error: false

      - name: OPA Test
        id: opa-test
        run: |
          opa test modules/ecs/policy/ -v
        continue-on-error: true

  validation-summary:
    name: Validation Summary
    runs-on: ubuntu-latest
    needs: [terraform-module-validation, opa-terraform-module-validation]
    if: always()
    
    steps:
      - name: Check validation results
        run: |
          if [ "${{ needs.terraform-module-validation.result }}" != "success" ] || [ "${{ needs.opa-terraform-module-validation.result }}" != "success" ]; then
            echo "❌ Validation failed"
            exit 1
          else
            echo "✅ All validations passed"
          fi

#############################################################################################################################
#####
##### COPY CODE THE CODE BELOW INTO TERRAFORM PLAN AND TERRAFORM APPLY GITHUB ACTIONS
#####
#############################################################################################################################
      
##      - name: Setup OPA
##        uses: open-policy-agent/setup-opa@v2
##        with:
##          version: latest
      
##      - name: OPA Policy Check
##        id: policy-check
##        run: |
##          cd environments/dev
##          opa eval -d opa-policies/service_ecs_policies.rego -i tfplan.json --fail "count(data.terraform.aws.ecs.deny) > 0"
##        continue-on-error: false
